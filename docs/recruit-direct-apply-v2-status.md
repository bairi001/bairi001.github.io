# Recruit Direct Apply V2 状态

- 前端直投运行时：已编写并写入正式 `/exec` 端点
- 独立 Apps Script 后端：已部署为 Web App v1
- `doGet()` 健康检查：已通过
- 自动化 `doPost()`：已返回 `ok:true`
- 重复提交保护：同一测试申请第二次返回 `duplicate:true`
- 独立招聘工作表写入路径：自动测试已通过请求处理；待店铺人工确认表格行
- 店铺通知邮件：代码已执行；待店铺人工确认收件
- 申请人邮件回执：尚未以真实测试邮箱确认
- 招聘岗位上下文预选：已完成
- `CONFIG.enabled`：true（当前仅在功能分支）
- 官网直投：尚未合并 main，尚未确认线上部署

只有在人工确认招聘表格、店铺通知邮件和申请人回执，并完成 main 合并及线上测试后，才可记录为“招聘直投已正式上线”。
